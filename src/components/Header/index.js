import React from "react";

const Header = () => (
  <div className="jumbotron p-3">
    <h1 className="text-center">Parks & Rec Clicky Game!</h1>
    <h3 className="text-center">Click on a character to earn points, but don't click on any more than once!</h3>
  </div>
)
export default Header;